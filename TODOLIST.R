# New set-up:
#   Latex2html only does the html step, not the escape step
#   Write mb routine 
#   write single blank routine
#   write pulldown and multiple pulldown routine
#   write formula question routine
#   add new question types to maketestquiz
#   make library
#   disentange aidfunctions and other cores in separate files
#   Check everywhere that sampling from a
#       a random number 1...int(scalar); 
#       use vectorsample()
#   iAlt in answer_select works incorrectly, if I give 7, it gives 8 options plus noneoftheabove
#   put check on non-asci in html checker
#   add requires to individual routines
